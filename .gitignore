__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/
.venv/
.DS_Store

# DuckDB database (user data, not source)
*.duckdb
*.duckdb.wal

# Never commit credentials
config.json
*.api_key
*.inst_token

# Local agent/session state
.omc/
.claude/settings.local.json
# Agent worktrees live inside the repo; they are checkouts of this same
# repository, so linters and greps would otherwise scan a second copy.
.claude/worktrees/
